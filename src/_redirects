# 1. Physical assets (Let these pass through cleanly)
/watches/css/*  /watches/css/:splat  200
/watches/js/*   /watches/js/:splat   200

# 2. Dynamic UUID catch-all (Only needed for paths that don't match real files)
/watches/*      /watches/index.html  200
